// ─── Self-hosted fonts (woff2 only, latin subset, no external requests) ─────
import './assets/fonts.css'


import './assets/main.css'

import { createApp } from 'vue'
import App from './App.vue'

createApp(App).mount('#app')
